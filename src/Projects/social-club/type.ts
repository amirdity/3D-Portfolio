// import { type PaletteMode } from "@mui/material/styles/createPalette";

import { PaletteMode } from "@mui/material";

export type SidebarType = {
  mode: PaletteMode;
  setMode: (arg: PaletteMode) => void;
};
export type _PaletteMode = PaletteMode;
